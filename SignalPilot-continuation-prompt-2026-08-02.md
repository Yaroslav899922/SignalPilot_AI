# SignalPilot — continuation prompt 2026-08-02 (вечір)

Якщо сесія обірвалась — прочитай цей файл, CLAUDE.md і HYPOTHESES.md, і продовжуй звідси.

## Стан на зараз

- Гілка: `fix/measurement-integrity`, ~19 локальних комітів, **НЕ запушено** (push робить користувач вручну: `cd D:\Projects\SignalPilot; git push`).
- Тести: 196 passed (включно з 9 нових test_rig_swing.py).
- Дані: `data/ohlcv/` оновлені до 2026-08-02 12:00 UTC (коміт `c37dbdd`), 12 монет, без пропусків.
- Гіпотеза №6 `swing_v1` — ПРОЙШЛА ВОРОТА по paired edge (train +0.208R CI[+0.125,+0.308] n=264; test +0.240R). АЛЕ expectancy ≈ 0 — перевага механіки входу, не прибутковість. Звіт: `reports/rig-swing-2026-08-02.md`.
- Пісочниця НЕ має доступу до Binance — свіжі дані качає користувач через `scripts/download-history.bat`.
- Монт D:\ глючить: git-локи прибирати через дозвіл на видалення; код правити через bash + перевірка null bytes == 0; тести ганяти в /tmp/sp.

## ПОГОДЖЕНИЙ ПЛАН (користувач: "все погоджено, виконуй по порядку")

Після КОЖНОГО кроку: запис у Notion (база "📓 Журнал розробки (нова)", data_source 9ee4eb77-e313-4a8b-a97d-0e5dfe5a950f) + оновити цей файл.

### Крок 1 — Нові Telegram-повідомлення (В РОБОТІ)
Джерело правди: `SignalPilot-Messages-Mockup-2026-08-02.md` (ПОГОДЖЕНО, розділ 7 = вимоги):
- 🔄 ПЛАН ОНОВЛЕНО замість пари "СКАСОВАНО + нове" при тому самому напрямку; явне "рівень $X → $Y" + причина людською мовою. Повідомляти ЩОРАЗУ (рішення 7.1).
- Рядок контексту ринку в кожному повідомленні: `Ринок 4h: … · BTC, ETH і SOL в один бік` (рішення 7.2, варіанти формулювань у макеті).
- ❌ СКАСОВАНО лише коли план реально вмирає (умова скасування / час вийшов); 🔁 НАПРЯМОК ЗМІНИВСЯ — окремий статус.
- Перевірки: повний список 6 рядків у первинному сигналі та при готовності до входу; короткий рядок "Готовність: 4 з 6 · чекаємо X" в ОНОВЛЕНО і зведеному плані (рішення 7.3).
- Шапка 📉 РИНОК у зведеному плані (brief).
ВАЖЛИВО: міняти ТІЛЬКИ шар повідомлень (тексти/вибір що слати). Журнал подій, event_id, стани — НЕ ЧІПАТИ (цілісність вимірювання v3.1!). Ключові місця: `actionable.py` рядок ~288 (порожня фраза "Система знайшла новий сценарій"), ~487 (формат), ~1109 (статуси/емодзі), `brief.py` (зведений план).
Цикл: правки в /tmp/sp → тести → синхронізація на монт → коміт → користувач робить push → ручний запуск GitHub Actions → перевірка Telegram + логи.

### Крок 2 — swing_v2 (виходи), гіпотеза №7
Джерело правди: `SignalPilot-SwingV2-Exits-Spec-2026-08-02.md` (користувач зняв паузу командою "все погоджено").
- Зареєструвати №7 у HYPOTHESES.md ДО прогону.
- Ядро swing_v2_trail: без фіксованої цілі, трейлінг-стоп за підтвердженими 4h-свінгами (буфер 0.25·ATR14 бару підтвердження, стоп тільки в бік прибутку), тайм-аут 30 діб. Варіанти: 3R, 4R.
- Контроль: сам swing_v1 на тих самих подіях; edge = net_R(v2) − net_R(v1) на тій самій угоді.
- Ворота: n≥30 train, month-CI нижня межа > 0 train, знак на test, ПЛЮС expectancy ядра > 0 на train І test.
- Обов'язково тест-паритет: v2-симулятор із вимкненим трейлінгом і ціллю 2R = результат основного движка байт у байт.
- Періоди/витрати/сесії як у swing_v1 звіті (train 2024-11…2025-10, test 2025-11…2026-06, 0.15% RT, visible).

### Крок 3 — Cohort analyzer для v3.1 (офлайн)
Джерело правди: `SignalPilot-v3.1-Measurement-Protocol-2026-08-02.md`, розділи 2, 7, 8:
- Extractor: фільтр подій (detected_at ≥ measurement_start_utc, policy_version=v3.1, повний provenance); популяції pending/barrier/timeout/terminal/paired.
- Confirmatory analyzer: 7-денний circular moving-block bootstrap, NumPy PCG64 seed 20260802, 50,000 реплік, перевірка концентрації; вердикт за розділом 8.
- Синтетичні fixtures + тести. Це розблоковує cutover (деплой Apps Script → Python → фіксація measurement_start_utc) — сам деплой ще ОКРЕМА команда.

### Крок 4 — Merge + push
- Локальний merge fix/measurement-integrity → main.
- Push робить користувач вручну (пісочниця не має логіна GitHub).
- Після push: ручний запуск GitHub Actions → перевірка Telegram-повідомлень і логів (для кроку 1).

## Прогрес (оновлювати після кожного кроку)

- [x] Крок 1: повідомлення — КОД ГОТОВИЙ, коміт `2c6630b`, 210 тестів passed. Зроблено: ПЛАН ОНОВЛЕНО (одне повідомлення замість пари), НАПРЯМОК ЗМІНИВСЯ, рядок контексту ринку (двопрохідний setup-check у cli.py), змішаний чекліст (повний у первинних/готових, короткий "Готовність: X з Y" в оновленнях і брифі), шапка РИНОК у брифі, конкретні причини скасувань. Журнал/event_id НЕ чіпались (тільки шар доставки). Живе ввімкнення — після push у кроці 4 (Actions → Telegram → логи).
- [x] Крок 2: swing_v2 — ЗАВЕРШЕНО, гіпотеза №7 **ЗАКРИТА** (коміт `1077f09`, 215 тестів passed). Ядро trail провалило ворота рішуче: expectancy train −0.485R / test −0.581R, edge vs v1 −0.50/−0.62R (CI повністю нижче 0). Діагноз: свінги n=2 підтверджуються надто часто → стоп впритул, 100% виходів по стопу. 3R/4R не допомагають. Урок у parking lot HYPOTHESES.md (вільніший трейл n=3–5 / після +1R / chandelier — ОКРЕМА гіпотеза, не запускати без погодження). Код: rig/swing_exits.py (паритет із движком закріплено тестом), rig/swing2_report.py, звіт reports/rig-swing2-2026-08-02.md.
- [x] Крок 3: cohort analyzer — ЗАВЕРШЕНО (коміт `4ed5fa7`, 224 тести passed). Модуль `src/signalpilot/cohort_analysis.py`: extract_cohort (правило включення §2 + популяції §7), block_bootstrap_ci (§8: 7-денні кругові блоки, PCG64 seed 20260802, 50k реплік, event-weighted mean), concentration_checks, confirmatory_verdict (passed/closed/inconclusive/insufficient_data), build_report. Edge перераховується з result_R−baseline_R, розбіжності зі збереженим edge_R позначаються. 9 тестів на синтетичних fixtures. Крок 8 порядку cutover розблоковано; measurement_start_utc ЛИШАЄТЬСЯ TBD до живого деплою (окрема команда).
- [x] Крок 4: merge — ЗРОБЛЕНО локально (fast-forward, main = fix/measurement-integrity = `ca4716e`+, 28 комітів). **PUSH ЧЕКАЄ КОРИСТУВАЧА**: `cd D:\Projects\SignalPilot; git push origin main`. Після push: вручну запустити GitHub Actions (market-brief.yml) → перевірити нові повідомлення в Telegram (ПЛАН ОНОВЛЕНО, контекст ринку, шапка РИНОК) → глянути логи. Якщо щось зламалось — відкат: користувач робить `git revert` або повідомляє сюди.

## Що лишилось у черзі ПІСЛЯ цієї сесії (за окремими командами)

1. Перевірка нових повідомлень наживо (після push + Actions).
2. v3.1 cutover за розділом 10 протоколу: деплой Apps Script (Chrome, редактор Monaco — нотатки в Notion 2026-07-19) → перевірка аркушів/scheduler_runs → пара STARTED→FINISHED → фіксація measurement_start_utc у протоколі. Лише за командою "погнали на деплой".
3. Можливі нові гіпотези з parking lot (BB-squeeze + swing; вільніший трейл) — тільки після пре-реєстрації.

## Ключові коміти сесії 2026-08-02

`153530e` спека swing_v1 → `5f93da8` фільтри → `ada1185` реєстрація №6 → `7051978` swing-рука+звіт → `fdcb1f4` спека №7 → `c37dbdd` свіжі дані → `7723c3b` макет повідомлень → `8178f35` погоджені рішення → цей файл.

## Правила спілкування з користувачем

- Не програміст; без сленгу і проф. термінів ("спека" → "специфікація"). Пояснювати простими словами.
- Він торгує руками; система лише знаходить сетапи й повідомляє. SignalPilot НІКОЛИ не торгує сам.
- Після кожної справи — запис у Notion. Питання ставити текстом у відповіді (інтерактивні питання інколи не доходять).
