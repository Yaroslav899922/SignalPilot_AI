# SignalPilot — continuation prompt (2026-06-21, вечір 2: RIG + стратегії)

Продовжуємо роботу над D:\Projects\SignalPilot.

Я не програміст — пояснюй простими словами, маленькими кроками. Якщо я не кажу прямо "погнали", "працюємо" або не прошу внести правки — спочатку тільки обговорення.

## Як ми сюди дійшли (контекст цієї сесії)

Хотіли "полірувати торгові стратегії". Перед тим, як щось крутити, вирішили перевірити, чим узагалі міряємо результат — це бектест-движок RIG (`src/signalpilot/rig/`).

Що з'ясували:
- RIG був **свідомо виведений з проєкту** в коміті `301f15e` ("Live analyst: cleanup old logic"). Докази: тестові файли `test_rig_engine.py` і `test_signals.py` вихолощені з підписами "rig module is no longer part of SignalPilot". Стару функцію стратегії `build_signal` видалили, а сам код RIG лишили лежати на диску — тому він висів напівмертвий і **не запускався** (битий імпорт `build_signal` у `rig/plans.py`).
- Висновок: остання жива стратегія **жодного разу не була виміряна**. Полірувати наосліп = ризик зламати робоче. Тому вирішили спершу оживити RIG.

## Що зробили цієї сесії (тільки офлайн-вимірювання, живі сигнали НЕ чіпали)

- Оживили RIG: прибрали мертву breakout-руку (вона тягла видалену `build_signal`).
- Додали нову руку **Pifagor Strategy 1** (`pifagor_s1` у `rig/plans.py`) — механічне ядро вебінарної стратегії: імпульс із 2 свічок (друга оновлює екстремум, але не коригує далі 50% першої) → фіба LOY..HAI → один лімітний вхід на 50%, тейк 38.2%, стоп трохи за 61.8%. **Свідомо БЕЗ ladder-входів і БЕЗ "ракети" (докупівлі проти руху)** — це небезпечний механізм зливу.
- Додали відтворюваний раннер `src/signalpilot/rig/compare.py` (Pifagor vs baseline vs pullback).
- Додали точковий тест `tests/test_rig_pifagor.py`.

## Що перевірили

- Локальні тести: **64/64 OK**.
- RIG запускається з диску, `compare.py` генерує звіт `reports/rig-pifagor-2026-06-21.md`.

## Висновок бектесту (головне)

**Механічне ядро Pifagor S1 програє тупому входу (baseline) — стабільно:**
- Train: pifagor −0.280R vs baseline −0.037R → різниця **−0.244R**, month-CI [−0.384, −0.081] (повністю нижче 0).
- Test: pifagor −0.488R vs baseline −0.018R → різниця **−0.471R**, month-CI [−0.666, −0.265].
- По символах на test усі в мінусі (BTC −0.42, ETH −0.48, SOL −0.57).
- Win-rate вийшов **48–55%, а не 92%** із вебінару. Дрібний тейк (38.2%) дає погане RR — треба ~58%+ перемог лише щоб вийти в нуль.
- Заявлений "92%" у вебінарі тримається на відсутності стопів і "ракеті" — саме тому ми це не кодили.

**Критерій НЕ виконано → це ядро не варте розвитку в такому вигляді.** RIG спрацював як треба: за пів години замість місяців реальної торгівлі.

## Технічні нюанси для наступної сесії

- Файли на цьому диску (мережевий монт) **глючать з інструментом редагування** — три файли отримали null-байти й не імпортувались. Лікували: відновлення з git + перезапис чистим UTF-8 через bash/python. Урок: правки в `rig/` краще писати через bash, потім перевіряти `null bytes == 0` і проганяти тести.
- Видалення файлів на монті треба було окремо дозволити.
- **Зміни ще НЕ закомічені.** На диску лежать незакомічені: `rig/plans.py`, `rig/engine.py`, `rig/run.py`, новий `rig/compare.py`, новий `tests/test_rig_pifagor.py`, новий звіт. Якщо хочеш зберегти — треба commit (push/GitHub Actions для цього НЕ потрібні, бо RIG офлайн і не торкається Telegram).

## Важлива межа

RIG вирішує по **4h-свічці**. Твоя жива стратегія `breakout_retest` — трьохрівнева (4h/1h/15m). Тому **цей RIG ще не міряє точно живу стратегію** — це окрема більша задача (навчити RIG носити 1h+15m індикатори). Baseline і Pifagor (теж 4h) він міряє чесно.

## Поточний стан живої системи (НЕ змінювали цієї сесії)

- Активний workflow: `.github/workflows/market-brief.yml`.
- Session-based Telegram Market Brief за київськими часами (03:07, 11:07, 12:07, 17:07, 19:07, 00:07).
- Move Alert ±1.5% по 15m. Антиспам **свідомо відкладено** — спостерігаємо ~3 дні, потім рішення.
- Telegram HTML: `>` і `<` писати як `&gt;` / `&lt;`.

## Плани далі

1. Move Alert: подивитись ~3 дні реальні повідомлення, тоді вирішити антиспам (1 alert на монету/напрямок за сесію).
2. RIG-стратегії: спробувати **Стратегію 2 (Fib Div)** — тейки далі (−0.27/−0.618), RR кращий; АБО Pifagor на **денному ТФ** (докачати денні дані); АБО laddered-версія.
3. Велика задача: навчити RIG міряти живу `breakout_retest` (multi-TF).
4. Futures context (CoinGlass: funding/OI/long-short) — досі відкрито.
5. За потреби стиснути Market Brief.

## Останні важливі коміти (до цієї сесії)

- `446d78a` — Add session market briefs and move alerts.
- `e3799ca` — Fix market brief UTC schedules.
- `aa4e6ce` — Stabilize manual brief session labels.

Не змінюй код без мого "погнали" або "працюємо". Зміни в RIG — офлайн, Telegram/GitHub Actions для них не потрібні.
