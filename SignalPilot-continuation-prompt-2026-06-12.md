# Промт продовження роботи над SignalPilot (на 2026-06-12)

Продовжуємо роботу над `D:\Projects\SignalPilot`.

Я не програміст — пояснюй простими словами, маленькими кроками, один крок за раз; я пишу «готово» / «погнали». **Поки я прямо не скажу «погнали» — лишайся в режимі обговорення, нічого не будуй.**

## Де ми зараз (підсумок попереднього дня)

- Збудовано окремий **вимірювальний прилад** (evaluation rig) у `src/signalpilot/rig/`: `data → plan → fill/no-fill → trade result → metrics → comparison`. Це **не бот**, а лінійка для перевірки ідей.
- Порівняли 3 варіанти входу: `breakout-core (4h)`, `pullback v1`, `dumb baseline` — на замороженій історії Binance (BTC/ETH/SOL, 4h+15m, 2024-11 → 2026-06).
- **Pullback v1 закрито** за наперед заявленим критерієм (2 раунди + 2 незалежні код-рев'ю). Довше життя ліміту підняло fill-rate 6%→65%, але переваги над baseline не дало (пізні філи — збиткові). Закрито саме EMA50-відкат із цими правилами; EMA20/інша логіка — окрема майбутня гіпотеза.
- Прилад зміцнено: target не зараховується у свічці філа; timeout по open; train/test split; CI **різниці** `pullback − baseline`; month-block bootstrap; діагностика за віком філа. **67 тестів (10 на рушій), усі зелені.**

## Спочатку прочитай (щоб увійти в контекст)

- `RIG-REVIEW-NOTES.md` — стан рига, фінальне рішення, ключові нюанси.
- `reports/rig-comparison-v2-2026-06-11.md` — останній звіт із цифрами й вердиктом.
- `SignalPilot-v1-Measurement-Spec-4h-Pullback-Plans.md` — принципи вимірювання.
- Код рига: `engine.py`, `plans.py`, `metrics.py`, `run.py`, `dataset.py`; тести `tests/test_rig_engine.py`.

## Дисципліна, яку НЕ порушуємо (з обох рев'ю)

- Завжди є **dumb baseline**; рішення — по **різниці** `гіпотеза − baseline`, а не по «гіпотеза > 0».
- Рішення «чи користуватись» — лише по `live_visible_sessions` (Київ 07–23).
- Критерій рішення записуємо **наперед**, ДО перегляду test.
- train/test (ідеально — walk-forward або реально нові дані після 2026-06); test не чіпаємо до фінальної перевірки.
- Похибка — **month-block bootstrap** (BTC/ETH/SOL сильно корельовані).
- Жодного зазирання вперед; консервативні філи; одна угода на символ.
- **Одна ручка за раз.** Якщо гіпотеза не б'є baseline за межі похибки — чесно закриваємо, не мучимо.

## Що далі (обрати завтра, в обговоренні — не наперед)

1. **Нова гіпотеза з нуля** (не реанімація pullback), кожна — зі своїм pre-registered критерієм. Кандидати з рев'ю: breakout/retest, режимний фільтр (тренд vs боковик), стиснення→розширення волатильності, трендслідування з кращими виходами, або «сигнальний радар» без рекомендації входу.
2. Або **як слід зміряти `breakout-core`** — єдиний arm, що хилився в плюс (+0.096R), але на малій вибірці (77) і з CI через нуль.

## Окремо обговорити завтра (просто записано, рішення — завтра)

- **Запуск на реальних даних і TradingView.** Мета далі — рухатись від «вимірювання на історії» до роботи на живих даних. Можливе джерело/сигнали — **TradingView** (alerts/webhook) замість прямого Binance API. Завтра обговоримо: TradingView vs Binance; ручні алерти vs автоматизація; як це лягає на перехід «прилад → продукт». **Поки нічого не будуємо — спершу обговорення.**

## Перший крок завтра

Прочитай файли вище, підтверди, що ти в контексті, і запропонуй 1–2 конкретні варіанти наступного кроку (нова гіпотеза або breakout-core) — у режимі обговорення. Будуємо тільки після мого «погнали».
