# SignalPilot — Spec: Reversal Setup (liquidity-sweep + FVG + MSS)

*Чернетка на 2026-07-02. Базовий коміт: `f84d4fe`. Статус: ОБГОВОРЕННЯ — код ще не чіпаємо.*

Мета документа: перетворити картинку «Price action Concepts — Reversal Setup» на **однозначні механічні правила**, які можна прогнати через RIG проти baseline. Поки правило нечітке — воно не вимірюване, а отже за нашим законом «RIG як ворота» не існує.

---

## 0. Ідея сетапу простими словами

Даунтренд видихається, ціна робить діапазон. Потім робить фінальний прокол униз — **знімає ліквідність** (стопи) під лоу діапазону — і швидко вертається назад. Це вхід «туди, де великий гравець набрав позицію»: стоп тісний (під проколом), ціль велика (назад у діапазон і на нові хаї). Тобто **асиметрія ризик/прибуток вбудована в конструкцію** — саме те, чого бракувало Pifagor (той помер через дрібну ціль 38.2%).

Ключова відмінність від закритих гіпотез: вхід не «наосліп у зону», а **після підтвердження зламом структури (MSS)**.

---

## 1. Мова структури (фундамент для всіх правил)

### 1.1. Свінг (pivot) — параметр `N`
- **Swing high** на барі `i`: `high[i]` строго більший за `high` усіх барів у вікні `[i-N, i+N]`.
- **Swing low** на барі `i`: `low[i]` строго менший за `low` усіх барів у вікні `[i-N, i+N]`.
- **Чесність (no look-ahead):** свінг на барі `i` вважається *підтвердженим лише через `N` барів* (коли зʼявився правий бік вікна). Усі рішення приймаємо тільки за підтвердженими свінгами. Це вносить лаг `N` барів — і це правильно.
- Фіксуємо: **`N = 2`** (на 4h). Це єдина ручка детектора свінгів; змінювати лише окремим експериментом.

### 1.2. FVG (Fair Value Gap) — 3-свічковий розрив
- **Бичачий FVG** на трійці `(a, b, c)` підряд: `high[a] < low[c]`. Розрив = зона `[high[a], low[c]]` (неефективність, яку ринок схильний згодом заповнити).
- Фільтр шуму: зона зараховується, лише якщо її висота `>= FVG_MIN_ATR * ATR` (щоб не ловити мікророзриви).
- Фіксуємо: **`FVG_MIN_ATR = 0.10`**.

### 1.3. MSS (Market Structure Shift) — злам структури
- Бичачий MSS = 4h **close вище останнього підтвердженого внутрішнього swing high**, що сформувався в фазі проколу/розвороту.
- Це тригер підтвердження. Саме на барі MSS ми створюємо план.

---

## 2. Шість кроків сетапу — механічно

> Напрям опису — бичачий розворот (як на картинці). SHORT-дзеркало будуємо симетрично й тестуємо окремо.

**Крок 1 — Діапазон після даунтренду.**
Контекст даунтренду: на момент формування — `close < ema50 < ema200` на 4h (той самий `trend()`, що вже в коді). Діапазон задають два підтверджені свінги: `range_low` (RL) = свінг-лоу, `range_high` (RH) = наступний свінг-хай над ним. Умова існування: `RH > RL` і між ними щонайменше `MIN_RANGE_BARS` барів.

**Крок 2 — Untapped range low.**
RL «незнятий», якщо після його формування жоден бар не закрив і не проколов його низом аж до моменту sweep. Тобто RL — свіжа ліквідність. Трекаємо прапорець «untapped» у стані.

**Крок 3 — Liquidity sweep (зняття лоу).**
Sweep-бар: `low < RL` (прокол гнотом униз) **і** `close > RL` (повернення в діапазон тим самим 4h-баром). Найнижча точка = `swept_low`. Sweep має статись у межах `SWEEP_WINDOW` барів від формування RL (інакше це вже інший ринок).

**Крок 4 — Fib як міра глибини (не обовʼязковий тригер, а фільтр).**
Від ноги `RH -> RL` рахуємо 1.618-розширення вниз: `fib_1618 = RL - 0.618 * (RH - RL)`. Sweep вважаємо «якісним», якщо `swept_low` дійшов у зону `[fib_1618, RL]` (ліквідність знята до виміряного рівня, а не випадковий тик). Прапорець `fib_ok`.

**Крок 5 — FVG підтверджує рух угору.**
Під час/після sweep у русі вгору має зʼявитись бичачий FVG (за 1.2). Він виконує дві ролі: (а) підтвердження неефективності/імпульсу, (б) **зона входу**. Беремо перший валідний FVG після `swept_low`.

**Крок 6 — MSS + вхід + вихід.**
Коли після sweep стається бичачий MSS (за 1.3) — план активний:
- **Entry (limit):** середина FVG-зони: `entry = (fvg_low + fvg_high) / 2`.
- **Stop:** трохи під проколом: `stop = swept_low - STOP_BUFFER_ATR * ATR`.
- **Target 1 (базовий для R):** `RH` (повернення у верх діапазону).
- **Target «нові хаї»** (діагностика розширеного ходу): `RH + (RH - RL)` — вимірений хід. Для критерію R беремо T1; T2 лише як довідку.

---

## 3. Зафіксовані параметри (наперед, щоб не підганяти під картинку)

| Параметр | Значення | Роль |
|---|---|---|
| `N` (свінг-вікно) | 2 | детекція свінгів на 4h |
| `FVG_MIN_ATR` | 0.10 | мінімальна висота FVG |
| `MIN_RANGE_BARS` | 3 | мінімальна ширина діапазону |
| `SWEEP_WINDOW` | 12 | макс. барів від RL до sweep |
| `STOP_BUFFER_ATR` | 0.25 | буфер стопа під проколом |
| Entry | FVG midpoint | лімітний вхід |
| Target 1 (для R) | `RH` | базова ціль |
| Timeout | 12×4h | успадковано з рушія |
| Одна угода/символ | так | успадковано з рушія |
| Fill/ведення | 15m | успадковано з рушія |

**Правило проти перепідбору:** ці числа заморожуються ДО першого прогону. Будь-яка зміна — окремий іменований експеримент з власним звітом, а не «докрутка».

---

## 4. Таймфрейм і як воно лягає в наявний RIG

- **Структура й рішення — на 4h** (щоб перевикористати `dataset.py`, `engine.py`, комісії, сесії, критерій — усе вже готове й перевірене).
- **Філи й ведення — на 15m** (як у всіх наявних руках).
- Відмінність від pullback/pifagor: reversal — **стан-машина**, а не «один план на кожен 4h-бар». Тому нова логіка живе у `build_decisions`: проганяємо 4h-бари, тримаємо стан (`range -> untapped -> sweep -> fvg -> mss`) і **емітимо `Plan` лише на барі, де MSS підтвердив**. Далі — той самий `simulate_plans`, нічого в рушії міняти не треба. Це важливо: механіка філа/стопа/цілі лишається спільною → порівняння чесне.

---

## 5. Що НАВМИСНО спрощуємо (механічне ядро, не повна дискреційна версія)

- Один вхід (FVG midpoint), без ladder і без докупівлі проти руху.
- FVG як єдина зона входу (не моделюємо окремо order block / demand-блок понад FVG).
- Без мультитаймфреймової дискреційної конфлюенції.
- Структура — лише за підтвердженими свінгами `N=2`; жодного «на око».
- Це, як і з Pifagor, **консервативне ядро ідеї**. Якщо ядро дає edge — розширюємо; якщо ні — це чесний результат по ядру, а не присуд усій школі.

---

## 6. Як міряємо — критерій воріт (той самий, що для pullback і pifagor)

Залишаємо й розвиваємо `reversal_v1`, ЛИШЕ якщо різниця `reversal - baseline`:
1. **додатна** на train;
2. її **month-block CI вище 0** на train (нижня межа > 0);
3. **стабільна по символах** (BTC/ETH/SOL усі не проти);
4. **підтверджена на test**.

Якщо хоч одна умова провалилась — рука не робоча, закриваємо (як Pifagor).

**Окрема засторога по вибірці:** розвороти рідкі → угод буде мало → CI широкий. Якщо `resolved < ~30` на символ — результат діагностичний, не присуд; тоді розширюємо набір символів, а не тюнимо параметри.

---

## 7. План дій (поетапно, кожен крок — офлайн, з тестами, з локальним комітом)

Робимо маленькими кроками. Після кожного — тести зелені + локальний commit (бекап). Без push. Код рушія (`engine.py`) не чіпаємо.

**Етап 0 — цей документ.** Ти критикуєш, узгоджуємо визначення й числа. ← ми тут.

**Етап 1 — детектор свінгів.** Нова чиста функція (`rig/structure.py`): підтверджені swing high/low з лагом `N`. Юніт-тести на синтетичних рядах (зигзаг, плато, рівні бари). Ціль: 100% детермінізм, нуль look-ahead.

**Етап 2 — детектор FVG.** Функція пошуку бичачих/ведмежих FVG з фільтром `FVG_MIN_ATR`. Тести на ручних трійках свічок (є розрив / немає / замалий).

**Етап 3 — стан-машина сетапу.** `range -> untapped -> sweep -> fib_ok -> fvg -> mss`. Тести: (а) ідеальна послідовність дає план; (б) зламана послідовність не дає; (в) зафіксований `swept_low`, `entry`, `stop`, `target` рахуються правильно.

**Етап 4 — нова рука `reversal_v1`.** Вбудувати стан-машину у `build_decisions`, емітити `Plan` на барі MSS. Прогнати наявні тести рушія — мають лишитись зелені (ми його не міняли).

**Етап 5 — RIG-прогін + звіт.** `reports/rig-reversal-<date>.md`: train/test, по символах, fill-rate, різниця vs baseline з month-CI. Присуд за критерієм розділу 6.

**Етап 6 — рішення.** Проходить ворота → обговорюємо винесення в живий бриф як 5-й сценарій. Не проходить → чесно закриваємо ядро, лишаємо запис у RIG-REVIEW-NOTES.

---

## 8. Відкриті питання (вирішити ДО коду, на етапі 0)

1. **Таймфрейм структури:** 4h (менше build, менше сигналів) чи 1h (більше сигналів, ближче до інтрадей-стилю картинки)? Я схиляюсь до 4h заради перевикористання рушія — але це головна розвилка.
2. **`swept_low` як стоп проти fib-стопа:** ставити стоп під проколом (тісно) чи під `fib_1618` (ширше, але менше вибивань)? Пропоную почати з проколу — тісний стоп = кращий R.
3. **Target 1 = RH чи fib-розширення вгору?** Для чесного R беру RH (консервативно). Згода?
4. **Скільки символів?** Через рідкість сигналів, можливо, одразу розширити з 3 до умовно 5–8 монет, щоб набрати вибірку. Це вимагає докачки даних (`rig/download.py`), Binance з пісочниці заблокований 451 — качати тобі локально.

---

## 9. Ризики (чесно)

- **Перепідбір:** багато ручок (свінг, sweep, FVG, MSS) → легко намалювати правила під приклад. Захист: параметри заморожені наперед (розділ 3), train/test, критерій наперед.
- **Субʼєктивність структури:** «злам структури» у трейдерів на око; наша механічна версія може ловити не те, що людина. Це фіча вимірювання, не баг — але висновок стосується саме нашого визначення MSS.
- **Мала вибірка:** розвороти рідкі → широкий CI → можлива «нічия», а не чистий висновок. План Б у розділі 6.
- **HTF-природа:** автор price-action-школи часто мислить день/тиждень; на 4h геометрія може бути іншою (те саме обмеження було в Pifagor-звіті).
